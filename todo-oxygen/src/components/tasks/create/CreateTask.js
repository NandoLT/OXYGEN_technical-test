import React from 'react';
import Layout from '../../commons/layout/Layout';

export default function CreateTask() {
    return (
        <Layout>
            <div>Formulario para añadir nueva tarea</div>
        </Layout>
    )
}
